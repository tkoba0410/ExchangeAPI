# Stage9

## Stage9 の目的

Stage9 は、構造上の曖昧な概念を排除し、API の回帰保証を可能にする検査基盤を確立するフェーズである。

Exchange 拡張や機能拡張ではなく、

* ExchangeInfo 概念の廃止
* API テスト体系の確立（静的／動的）
* レビュー運用の再設計

に集中する。

---

# Stage9 の範囲

---

## 9-1 ExchangeInfo 概念の廃止

ExchangeInfo という「取引所横断の塊概念」を廃止する。

対象は型名ではなく、

* 取引所差分を横断的な情報塊として扱う構造
* Core / Facade が交換所固有事情を包含する構造
* 実行時に大きな情報塊を前提とする設計

である。

### 方針

* 取引所差分は部品として分解する
  （Signer / Canonicalizer / EndpointCatalog 等）
* Facade は薄い依存のみを受け取る
* 取引所固有事情は Exchange モジュール内部に閉じ込める
* Core は exchange 非依存を厳守する
* 横断的「まとめ情報」概念は設計上禁止する

ExchangeInfo 相当の抽象概念は設計規約上で廃止する。

---

## 9-2 API 静的テスト基盤の整備

API 実行を伴わずに検査可能な構造を整備する。

目的は：

* リクエスト組み立ての正当性検査
* 署名処理の検査
* エンドポイント解決の検査
* Canonical 化ロジックの検査

### 実装範囲

* Fake Transport
* 署名結果の検査フック
* Request Snapshot 検査機構
* EndpointCatalog の整合検査

CI では静的テストのみ常時実行する。

ネットワーク接続は不要とする。

---

## 9-3 API 動的テストの段階導入

実 API を用いた動的テストの導入方針を整理する。

### 原則

* CI には含めない（初期段階）
* 手動実行を基本とする
* 送受信ログは回帰資産として保存可能にする
* 実地試験は限定的・段階的に行う

### 対象

* Public API
* Private API（最小限）

大量データ収集や常時実行は Stage9 の範囲外とする。

---

## 9-4 レビュー体制の再構築

Stage9 の構造変更と保証体系に合わせ、レビュー体制を再整理する。

### 観点分類

1. 構造レビュー（ExchangeInfo 依存の残存検査）
2. 静的保証レビュー（テストで検査可能か）
3. 層責務レビュー（Core / Exchange / Facade 境界）
4. 依存方向レビュー（逆流の禁止）
5. DTO / ValueObject 境界レビュー

### 運用整備

* PR テンプレート更新
* docs/reviews 再整理
* Normative / Reference の区分明確化
* Stage 単位レビュー観点定義

---

# Stage9 の非目的

Stage9 では以下を行わない。

* 新規 Exchange 追加
* 既存 Exchange の endpoint 拡張
* Contracts の意味拡張
* 本格的なトレーディング運用
* 大量実地データ運用基盤の確立

---

# Stage9 の到達状態（イメージ）

* ExchangeInfo 相当の横断塊概念が設計上消滅している
* 取引所差分は部品化されている
* API 組み立てはネットワーク不要で完全検査可能
* CI は静的保証を常時実行している
* 動的テストは段階導入方針が確立している
* レビュー観点が構造と整合している

---

各 9-x の詳細（DoD・成果物・手順）は、着手時点で個別に定義するものとする。
